import MemoryBookClient from "@/components/memory-book/memory-book-client"

export const metadata = {
  title: "Memory Book | BringBack Dashboard",
  description: "An interactive tactile scrapbook inside the protected BringBack dashboard.",
}

export default function MemoryBookPage() {
  return <MemoryBookClient />
}
